import { SimplePage } from '@/components/simple-page';

export default function MembersPage() {
  return (
    <SimplePage
      title="Members"
      description="Member search and profile discovery will be expanded from the backend profile APIs."
    />
  );
}
