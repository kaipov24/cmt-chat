import { SimplePage } from '@/components/simple-page';

export default function AboutPage() {
  return (
    <SimplePage
      title="About"
      description="CMT Community Platform is for community support and does not provide medical advice."
    />
  );
}
