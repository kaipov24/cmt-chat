import { SimplePage } from '@/components/simple-page';

export default function LoginPage() {
  return (
    <SimplePage
      title="Login"
      description="Login UI will be connected to the Phase 3 authentication API in a later frontend pass."
    />
  );
}
