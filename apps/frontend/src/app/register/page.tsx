import { SimplePage } from '@/components/simple-page';

export default function RegisterPage() {
  return (
    <SimplePage
      title="Register"
      description="Registration UI will be connected to the Phase 3 authentication API in a later frontend pass."
    />
  );
}
