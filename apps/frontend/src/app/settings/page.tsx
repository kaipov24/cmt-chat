import { SimplePage } from '@/components/simple-page';

export default function SettingsPage() {
  return (
    <SimplePage
      title="Settings"
      description="Profile and location visibility settings will be connected to the backend profile API."
    />
  );
}
